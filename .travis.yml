sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t paddy27/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true paddy27/react-test npm test

after_success:
  - docker build -t paddy27/multi-client ./client
  - docker build -t paddy27/multi-nginx ./nginx
  - docker build -t paddy27/multi-server ./server
  - docker build -t paddy27/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push paddy27/multi-client
  - docker push paddy27/multi-nginx
  - docker push paddy27/multi-server
  - docker push paddy27/multi-worker
